# The deref-rooted shapes close: BRIDGE 1S, and both mixed-chain leaves

[FACT] Both canonical proj⊗deref shapes are CLOSED:
- `const_write_proj_deref_simulation` — `(*p).f := v` over ANY load
  spine: fragment `[spine; Load; Borrow(Mut); CStore; Die]`, the spine
  mother lemma for the prelude and the C1 endgame with the parent tag
  from the LOADED pointer (via `MemValSim`) instead of a local binding.
- `const_write_deref_proj_simulation` — `*(s.f) := v`, the pointer FIELD
  of a bound tuple local: fragment `[Borrow(Shared); Load; Die; CStore]`.
`const_write_deref_nonspine_simulation` is now a PROVED dispatcher.
Audit stays 4 by the usual accounting (deep chains + narrowed
proj-nonlocal + copy + ref-place), with strictly more closed.

[FACT] BRIDGE 1S (`sb_ref_read_die_cancels`, keystone.lean): the
read-side keystone. `Borrow(Shared); read via the fresh tag; Die` has
exactly the stack effect of the bare parent read — which is the read
mirlite's `resolvePlaceAcc` performs when it dereferences a projected
pointer place, so the two machines' event streams reconcile with no new
invariant. Phase 2 is the structural delta from the Mut keystone: a read
through a fresh `Ref` on TOP of the stack is a no-op (nothing above it to
disable; `readCellContent_top_ref`), where the Mut keystone's phase 2
rewrites each cell to itself. Generated by systematic adaptation of the
Mut proof — the fold machinery is op-agnostic enough that the mirror
compiled after only the empty-freeze-mask reduction was added.

[FACT] The supplier pattern held for the third time:
`sb_ref_Shared_ok_of_sb_read_ok` mirrors the Mut version (per cell
`readCell; pushCell`, and the push cannot fail). Every keystone/bridge
needs its target-side "succeeds-when" companion because the target
performs an event the source does not.

[FACT] `LoadSpine` is POINTER-TYPED only, so a tuple-typed proj base can
never be a spine — `*(s.f)`'s base must be handled as a bare local, and
the spine lemma applies only on the OTHER side (`(*p).f`, where the
spine is under the deref). The two leaves are not symmetric in what they
reuse, even though the fragments look like mirror images.

[OBS 2026-08-27] A tooling wound, self-inflicted and worth the scar
tissue: a chunked edit that spliced "from this sorry to the next
`· simp at h_w`" DELETED an entire adjacent theorem
(`const_write_proj_deref_simulation`) — the end marker matched inside
the NEXT theorem, and the build stayed green because the file remained
well-formed minus one theorem. Caught only by the audit's grep showing
the name missing. Reassembled from the session's applied patches. Rules:
(1) after any slice-edit, grep the file for the THEOREM NAMES that
should exist; (2) end markers for splices must be unique in the file —
`· simp at h_w` is not.

[EMP] (Lean 4.28) recurring potholes, all repaid this session:
`subst`'s eliminated-side roulette (write counter chains name-agnostic:
`calc` with `_ := lemma h` holes, or plain `have`s per equality);
`omega` off `Word` projections (Nat lemmas: `Nat.le_of_add_le_add_left`,
`Nat.succ_le_of_lt`); record-literal fields must sit right of their
brace; term-position `by rw [...] at h; exact h` blocks are fragile —
hoist `rw ... at` to tactic position before chaining.

Validation: units 15/15 + 42/42, suite pass 82 | fail 0 | unsupported 41
(123), differential matched 82 | mismatch 0 | skipped 0. Closed leaves
and BRIDGE 1S axiom-clean (propext / Classical.choice / Quot.sound).

**References:** proof/compiler.lean (audit), proof/keystone.lean
(BRIDGE 1S), 2026-08-27-regime-c-closed.md,
2026-08-27-nested-proj-reassoc.md.
